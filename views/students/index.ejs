<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
    integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Registry</title>
</head>

<body>
  <div class="container">
    <h1 class="mb-4">Students Repo</h1>
    <a href="/students/new" class="btn btn-success">New student</a>

    <% students.forEach(student => { %>
    <div class="card mt-4">
      <div class="card-body">
        <h4 class="card-title"><%= student.title %> <%= student.surname %></h4>
        <div class="card-subtitle text-muted mb-2">
          <%= student.createdAt.toLocaleDateString() %>
        </div>

        <a href="students/<%= student.slug %>" class="btn btn-primary">Read More</a>
        <a href="students/edit/<%= student.id %>" class="btn btn-info">Edit</a>
        <form action="/students/<%= student.id %>?_method=DELETE" method="POST" class="d-inline">
          <button type="submit" class="btn btn-danger">Delete</button>
        </form>
      </div>
    </div>
    <% }) %>
  </div>
</body>

</html>
